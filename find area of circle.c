#include <stdio.h>

int main() {
    float radius = 7;
    float area = 3.14 * radius * radius;

    printf("Radius = %.2f\n", radius);
    printf("Area of Circle = %.2f", area);

    return 0;
}