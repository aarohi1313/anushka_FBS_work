#include <stdio.h>
int main()
{
	char ch = 'A' ;
	
	  if (ch >= 'A' && ch <= 'Z')
	 
	      printf("uppercase");
	      
	    else 
		     if(ch >= 'a' && ch <= 'z') 
		     
			 printf("lowercase");
		
		else 
			 
			 printf("not a alphabet");
			 

	
}