#include <stdio.h>

int main() {
    float base = 10;
    float height = 5;

    float area = 0.5 * base * height;

    printf("Base = %.2f\n", base);
    printf("Height = %.2f\n", height);
    printf("Area of Triangle = %.2f\n", area);

    return 0;
}