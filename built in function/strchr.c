#include <stdio.h>
#include <string.h>

void main()
{
	
	char str[] = "computer";
	
	if(strchr(str,'p')!= NULL)
     
	 printf("character is found");
	 
	else 
	     	
	    printf("character is not found");
	
}